global:
  environment: dev

backend:
  replicaCount: 1
  image:
    tag: "1.0.0"

frontend:
  replicaCount: 1
  image:
    tag: "1.0.0"

agent:
  replicaCount: 1
  image:
    tag: "1.0.0"
  env:
    marketDataProvider: finnhub

ingress:
  enabled: true
  className: ""
  host: ""

secrets:
  existingSecret: stock-pulse-secrets
