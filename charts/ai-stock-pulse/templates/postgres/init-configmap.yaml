{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ai-stock-pulse.fullname" . }}-postgres-init
  labels:
    {{- include "ai-stock-pulse.labels" . | nindent 4 }}
data:
  init.sql: |
{{ .Files.Get "files/init.sql" | indent 4 }}
{{- end }}
